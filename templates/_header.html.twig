<nav class="navbar border-bottom navbar-expand-lg">
    <ul class="navbar-nav mr-auto">
        <li class="nav-item active">
            <a class="nav-link" href="/">My Biking App</a>
        </li>
    </ul>
    <ul class="navbar-nav ml-auto align-items-center">
        {% if not app.user %}
            <li class="nav-item active">
                <a href="{{ path('app_login') }}" class="mr-3">Login</a>
            </li>
            <li class="nav-item active">
                <a href="{{ path('app_user_registration') }}" class="btn btn-sm btn-outline-primary">Sign up</a>
            </li>
        {% else %}
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                  Account
                </a>
                <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                    <a class="dropdown-item" href="{{ path('app_user_account') }}">Profile</a>
                    <a class="dropdown-item" href="{{ path('app_logout') }}">Logout</a>
                    {# <div class="dropdown-divider"></div>
                    <a class="dropdown-item" href="#">Something else here</a> #}
                </div>
            </li>
        {% endif %}
    </ul>
</nav>
